@model IEnumerable<WebApp.Models.STOCKTBL>

@{
    ViewBag.Title = "Index";
}

<h2>商品在庫</h2>
<p>
    @Html.ActionLink("新規在庫情報追加", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DEALID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GOODSID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STOCK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LASTUDT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DEALID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GOODSID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STOCK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LASTUDT)
        </td>
        <td>
            @Html.ActionLink("編集", "Edit", new { id=item.DEALID }) |
            @Html.ActionLink("詳細", "Details", new { id=item.DEALID }) |
            @Html.ActionLink("削除", "Delete", new { id=item.DEALID })
        </td>
    </tr>
}
</table>
<p>
    @Html.ActionLink("商品画面へ", "index", new {id = UrlParameter.Optional, controller = "GOODSTBLs" })
</p>
